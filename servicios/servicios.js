import './servicios.css';import '../src/shared/site.js';import '../src/shared/banner-title.js';import{mountShell}from'../src/shared/shell.js';mountShell('servicios');
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{threshold:.12,rootMargin:'0px 0px -7% 0px'});document.querySelectorAll('[data-reveal]').forEach(node=>observer.observe(node));
const journey=document.querySelector('.journey-map');const updateRoute=()=>{if(!journey)return;const rect=journey.getBoundingClientRect(),start=innerHeight*.66,progress=Math.max(0,Math.min(1,(start-rect.top)/(rect.height-innerHeight*.3)));journey.style.setProperty('--route-progress',`${progress*100}%`)};updateRoute();addEventListener('scroll',updateRoute,{passive:true});
if(!reduced){const visual=document.querySelector('[data-parallax]'),cargo=visual?.querySelector('.hero-cargo-placeholder');visual?.addEventListener('pointermove',event=>{const rect=visual.getBoundingClientRect(),x=(event.clientX-rect.left)/rect.width-.5,y=(event.clientY-rect.top)/rect.height-.5;cargo.style.transform=`perspective(900px) rotateY(${-6+x*7}deg) rotateX(${-y*5}deg)`});visual?.addEventListener('pointerleave',()=>cargo.style.transform='perspective(900px) rotateY(-6deg)');const steps=[...document.querySelectorAll('.console-flow article')];let active=0;if(steps.length)setInterval(()=>{steps[active].classList.remove('is-active');active=(active+1)%steps.length;steps[active].classList.add('is-active')},2200)}
