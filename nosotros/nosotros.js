import './nosotros.css';import '../src/shared/site.js';import '../src/shared/banner-title.js';import{mountShell}from'../src/shared/shell.js';mountShell('nosotros');
const revealObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');revealObserver.unobserve(entry.target);}}),{threshold:.14,rootMargin:'0px 0px -7% 0px'});document.querySelectorAll('[data-reveal]').forEach(node=>revealObserver.observe(node));
const countObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;const card=entry.target,output=card.querySelector('strong span'),target=Number(card.dataset.count),start=performance.now(),duration=target>100?1450:1050;const tick=now=>{const p=Math.min((now-start)/duration,1),e=1-Math.pow(1-p,3);output.textContent=Math.round(target*e).toLocaleString('es-PE');if(p<1)requestAnimationFrame(tick);};requestAnimationFrame(tick);countObserver.unobserve(card);}),{threshold:.4});document.querySelectorAll('[data-count]').forEach(card=>countObserver.observe(card));
