import './plataforma.css';import '../src/shared/site.js';import '../src/shared/banner-title.js';import{mountShell}from'../src/shared/shell.js';mountShell('plataforma');
const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const reveal=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;entry.target.classList.add('is-visible');entry.target.querySelectorAll('[data-counter]').forEach(el=>{const target=Number(el.dataset.counter),start=performance.now();const tick=now=>{const p=Math.min((now-start)/900,1);el.textContent=Math.round(target*(1-Math.pow(1-p,3)));if(p<1)requestAnimationFrame(tick)};requestAnimationFrame(tick)});reveal.unobserve(entry.target)}),{threshold:.12,rootMargin:'0px 0px -6% 0px'});document.querySelectorAll('[data-reveal]').forEach(el=>reveal.observe(el));
const buttons=[...document.querySelectorAll('[data-feature]')],panels=[...document.querySelectorAll('[data-panel]')];let active=0;const show=index=>{buttons[active]?.classList.remove('active');panels[active]?.classList.remove('active');active=index;buttons[active]?.classList.add('active');panels[active]?.classList.add('active')};buttons.forEach((button,index)=>button.addEventListener('click',()=>show(index)));if(!reduced)setInterval(()=>show((active+1)%buttons.length),4800);
document.querySelector('[data-scroll-demo]')?.addEventListener('click',()=>document.querySelector('#demo')?.scrollIntoView({behavior:reduced?'auto':'smooth'}));if(!reduced){const stage=document.querySelector('[data-tilt]'),windowEl=stage?.querySelector('.dashboard-window');stage?.addEventListener('pointermove',e=>{const r=stage.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;windowEl.style.transform=`rotateX(${2-y*2}deg) rotateY(${x*2}deg)`});stage?.addEventListener('pointerleave',()=>windowEl.style.transform='rotateX(2deg)')}
